=====
SHIMS
=====

A collection of lightweight executable wrapper scripts for third-party 
system packages managed via the installation scripts in 
`scripts/installer/` -- running `install-deps rehash` or individual installer
rehash commands rebuilds the executable shims in `scripts/installer/shims/bin/`.